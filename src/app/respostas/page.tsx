import { saveAnswer } from "@/app/actions";
import { Badge, Card, Empty, PageHeader } from "@/components/ui";
import { prisma } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function AnswersPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}) { const q=await searchParams; const answers=await prisma.answerEntry.findMany({orderBy:{updatedAt:"desc"}}); return <><PageHeader eyebrow="Reutilização consciente" title="Banco de respostas" description="Respostas sensíveis ou eliminatórias continuam exigindo revisão em cada candidatura."/>{q.erro&&<div className="notice">Preencha pergunta e resposta.</div>}<div className="grid two"><Card><h2>Nova resposta</h2><form action={saveAnswer} className="stack"><label>Pergunta<textarea name="question" required/></label><label>Resposta<textarea name="answer" required/></label><label className="checkbox"><input type="checkbox" name="sensitive"/> Contém dado sensível ou eliminatório</label><label className="checkbox"><input type="checkbox" name="confirmed"/> Resposta revisada por mim</label><button>Salvar</button></form></Card><Card><h2>Respostas salvas</h2>{answers.length===0?<Empty title="Banco vazio">Salve respostas factuais; não use este espaço para testes técnicos ou declarações pessoais automáticas.</Empty>:<div className="list">{answers.map(a=><div className="listItem" key={a.id}><div><div className="chips">{a.sensitive&&<Badge tone="warn">revisão obrigatória</Badge>}<Badge tone={a.confirmed?"good":"neutral"}>{a.confirmed?"confirmada":"rascunho"}</Badge></div><strong>{a.question}</strong><p>{a.answer}</p></div></div>)}</div>}</Card></div></> }
